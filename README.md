# mailchimp-node-demo
A quick demo of using direct API requests to Mailchimp in a node app 

## running this thing
1. get `npm install` to do its magic
2. run it with `node index.js`
3. go to /signup.html
4. add a superpower to that subscribers record at /update.html
